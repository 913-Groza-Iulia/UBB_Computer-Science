%compute the number of occurences of an element e in a list
%occ([]-list, _ -the element to give, 0-nr of occ)
%flow model = i,i,o

%base case
occ([], _, 0).

occ([H|T], X, No):-
   H = X,
   occ(T, X, NewNo),
   No is NewNo +1.

occ([H|T], X, No):-
   H \= X,
   occ(T, X, No).

%--------------------------------------------------
%if you use the cut(!):
occ([], _, 0).

occ([H|T], X, No):-
   H = X,!,
   occ(T, X, NewNo),
   No is NewNo +1.

occ([H|T], X, No):-
   %H \= X,
   occ(T, X, No).


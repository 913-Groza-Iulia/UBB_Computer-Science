% sum of elems in a list
%sum(L-list,S-return number)
% flow model (i(nput),o(utput)) (i,i)

sum([], 0).
sum([H|T], S):-
  sum(T, TS),
  S is H+TS.

%---------------------------------------------------

% or we can use a collector variable:
% sumC(L-list, C-collector, S-result)
% flow model: (i,i,o)

sumC([], C, C).
sumC([H|T], C, R):-
   NewC is C+H,
   sumC(T, NewC, R).

% insert an element E on position M in a list
% insertAtPos(L-list, R-element to insert, P-position, R-result list)
% flow model: (i,i,i,o)

insertAtPos([], _, P, []):-
  P>1.

insertAtPos(L, E, 1, [E|L]). % we put E at the beginning of the list 

insertAtPos([H|T], E, P, [H|TR]):-
  P>1,
  P1 is P-1,
  insertAtPos(T, E, P1, TR).
